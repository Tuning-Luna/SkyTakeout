<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace 必须和接口全限定类名一致 -->
<mapper namespace="com.tuning.mapper.CategoryMapper">

    <insert id="insertCategory" parameterType="com.Tuning.entity.Category" useGeneratedKeys="true" keyProperty="id">
        insert into category
        (type, name, sort, status, create_time, update_time, create_user, update_user)
        values (#{type}, #{name}, #{sort}, #{status}, NOW(), NOW(), #{createUser}, #{updateUser})
    </insert>
</mapper>