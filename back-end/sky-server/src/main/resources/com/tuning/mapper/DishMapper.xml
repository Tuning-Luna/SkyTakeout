<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace 必须和接口全限定类名一致 -->
<mapper namespace="com.tuning.mapper.DishMapper">
    <select id="countByCategoryId" resultType="java.lang.Integer">
        SELECT COUNT(id)
        FROM dish
        WHERE category_id = #{categoryId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dish (name, category_id, price, image, description, status, create_time, update_time, create_user,
                          update_user)
        VALUES (#{name}, #{categoryId}, #{price}, #{image}, #{description}, #{status}, #{createTime}, #{updateTime},
                #{createUser}, #{updateUser})
    </insert>

    <select id="pageQuery" resultType="com.Tuning.vo.DishPageQueryVO">
        SELECT dish.*,c.name as categoryName
        FROM dish LEFT JOIN category c on dish.category_id = c.id
        <where>
            <if test="name != null and name != ''">
                AND dish.name LIKE CONCAT('%',#{name},'%')
            </if>
            <if test="status != null">
                AND dish.status = #{status}
            </if>
            <if test="categoryId != null">
                AND dish.category_id = #{categoryId}
            </if>
        </where>
    </select>

    <select id="getById" resultType="com.Tuning.entity.Dish">
        SELECT *
        FROM dish
        WHERE id = #{id}
    </select>

    <delete id="deleteById">
        DELETE
        from dish
        WHERE id = #{id}
    </delete>

    <update id="update" parameterType="com.Tuning.entity.Dish">
        UPDATE dish
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="categoryId != null">
                category_id = #{categoryId},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="image != null and image != ''">
                image = #{image},
            </if>
            <if test="description != null and description != ''">
                description = #{description},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="updateUser != null">
                update_user = #{updateUser},
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="getByCategoryId" resultType="com.Tuning.vo.DishListVO">
        SELECT *
        FROM dish
        WHERE category_id = #{categoryId}
    </select>
    <select id="getBySetmealId" resultType="com.Tuning.entity.Dish">
        SELECT d.*
        FROM dish d
                 LEFT JOIN setmeal_dish sd on d.id = sd.dish_id
        WHERE sd.setmeal_id = #{setmealId}

    </select>
    <select id="list" resultType="com.Tuning.entity.Dish">
        SELECT * FROM dish
        <where>
            <if test="name != null">
                AND name like concat('%',#{name},'%')
            </if>
            <if test="categoryId != null">
                AND category_id = #{categoryId}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

</mapper>