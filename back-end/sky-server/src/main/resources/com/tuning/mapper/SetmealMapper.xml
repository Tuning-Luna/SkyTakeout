<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace 必须和接口全限定类名一致 -->
<mapper namespace="com.tuning.mapper.SetmealMapper">
    <select id="countByCategoryId" resultType="java.lang.Integer">
        SELECT COUNT(id)
        FROM setmeal
        WHERE category_id = #{categoryId}
    </select>

    <insert id="insert" parameterType="com.Tuning.entity.Setmeal" useGeneratedKeys="true" keyProperty="id">
        insert into setmeal
        (category_id, name, price, status, description, image, create_time, update_time, create_user, update_user)
        values (#{categoryId}, #{name}, #{price}, #{status}, #{description}, #{image}, #{createTime}, #{updateTime},
                #{createUser}, #{updateUser})
    </insert>

    <select id="pageQuery" resultType="com.Tuning.vo.SetmealVO">
        SELECT s.*, c.name as categoryName
        FROM setmeal s
        Left Join category c
        on s.category_id = c.id

        <where>
            <if test="name != null and name != ''">
                AND s.name LIKE CONCAT('%',#{name},'%')
            </if>
            <if test="status != null">
                AND s.status = #{status}
            </if>
            <if test="categoryId != null">
                AND s.category_id = #{categoryId}
            </if>
        </where>
        ORDER BY update_time DESC
    </select>
</mapper>